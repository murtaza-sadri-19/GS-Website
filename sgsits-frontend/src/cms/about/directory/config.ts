import { defaultAdministration, defaultTelephoneDirectory } from './mock'

export const directoryConfig = {
  id: 'about_directory',
  defaults: {
    administration: defaultAdministration,
    telephoneDirectory: defaultTelephoneDirectory,
  }
}

export default directoryConfig
