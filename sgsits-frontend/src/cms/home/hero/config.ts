import { defaultHeroConfig, defaultHeroTiles } from './mock'

export const heroConfig = {
  id: 'hero',
  defaults: {
    config: defaultHeroConfig,
    tiles: defaultHeroTiles,
  }
}

export default heroConfig
